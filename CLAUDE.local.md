* Working on implementing semantic features to TabPFN via a secondary CLIP model. Encountered performance regression: on main branch TabPFN training starts with ~1.1 batch loss and quickly achieves 0.82 validation ROC AUC within 10 epochs, but on "sem-prior-cleaned" branch with `--semantic-feature-p 0.0 --progress-bar --semantic-batch-monitoring False` batch loss starts at ~2.3, decreases slowly, and only reaches 0.5 validation ROC AUC after 10 epochs (no convergence).